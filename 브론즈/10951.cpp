#include <bits/stdc++.h>
using namespace std;

int a, b;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    while (cin >> a >> b)
    {
        cout << a + b << '\n';
    }
    return 0;
}