#include <bits/stdc++.h>
using namespace std;

string input;
stack<char> stk;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> input;

    for (int i = 0; i < input.length(); i++)
    {
    }
}