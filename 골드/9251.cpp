#include <bits/stdc++.h>
using namespace std;

string a, b;
int cnt = 0;
int lastIdx = 1000;
int arr[1000] = {
    0,
};

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> a >> b;

    cout << cnt << '\n';
}