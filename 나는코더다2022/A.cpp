#include <bits/stdc++.h>
using namespace std;

int n, k, x;

struct TeamMate
{
    int a;
    int b;

    TeamMate(int x, int y) : a(x), b(y) {}
};
vector<TeamMate> arr;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> n >> k >> x;

    int maxi = 0;
}